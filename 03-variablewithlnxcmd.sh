#!/bin/bash

D=$(date)
L=$(ls-l)
C=$(cat testfile)

echo ("Today date is ${D}")  #$D is denotes my date is printing.

echo ("my file list is ${L}")  #$L is denotes my file list is printing.

echo ("my create file is :: ${C}") #$c is denoted my created file is printing.

